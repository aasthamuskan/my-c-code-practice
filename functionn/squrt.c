#include<stdio.h>
#include<math.h>
int main(){
    int a;
    printf("enter a no: ");
    scanf("%d",&a);
    int root = sqrt(a);//why we inicilise sqrt by root why not we directly use
    //printf("the square root is : %d",root);
    printf("%d",sqrt(a));
    return 0;
    //code chal hi nahi raha
}